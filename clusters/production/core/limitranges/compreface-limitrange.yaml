---
apiVersion: v1
kind: LimitRange
metadata:
  name: default-limit-range
  namespace: compreface
spec:
  limits:
    - default:
        cpu: "500m"
        memory: "2Gi"
      defaultRequest:
        cpu: "100m"
        memory: "256Mi"
      type: Container
