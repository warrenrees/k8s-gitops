---
apiVersion: v1
kind: LimitRange
metadata:
  name: default-limit-range
  namespace: cert-manager
spec:
  limits:
    - default:
        cpu: "500m"
        memory: "512Mi"
      defaultRequest:
        cpu: "50m"
        memory: "128Mi"
      type: Container
