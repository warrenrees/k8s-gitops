---
apiVersion: v1
kind: LimitRange
metadata:
  name: default-limit-range
  namespace: internal-nginx
spec:
  limits:
    - default:
        cpu: "1"
        memory: "1Gi"
      defaultRequest:
        cpu: "100m"
        memory: "256Mi"
      type: Container
