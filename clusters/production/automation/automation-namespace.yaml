apiVersion: v1
kind: Namespace
metadata:
  name: automation
  labels:
    # esphome/frigate need hostNetwork + privileged for device access, which
    # the baseline Pod Security Standard forbids.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
