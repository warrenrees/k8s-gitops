apiVersion: v1
kind: Namespace
metadata:
  name: media
  labels:
    # Some media workloads use hostNetwork/hostPort (e.g. Plex), which the
    # baseline Pod Security Standard forbids — so this namespace runs privileged.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
