apiVersion: apps/v1
kind: Deployment
metadata:
  name: piper
  namespace: media
spec:
  selector:
    matchLabels:
      app: piper
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: piper
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
      - env:
        - name: TZ
          value: America\Toronto
        imagePullPolicy: IfNotPresent
        name: piper-k8s
        securityContext:
          allowPrivilegeEscalation: false
        image: rhasspy/wyoming-piper:2.3.1 # {"$imagepolicy": "flux-system:piper"}
        args:
        - --voice=en-us-ryan-medium
        ports:
        - containerPort: 10200
          protocol: TCP
        startupProbe:
          tcpSocket:
            port: 10200
          failureThreshold: 60
          periodSeconds: 5
        livenessProbe:
          tcpSocket:
            port: 10200
          periodSeconds: 20
        readinessProbe:
          tcpSocket:
            port: 10200
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        volumeMounts:
        - mountPath: /data
          name: piper-data
      hostname: piper
      volumes:
      - name: piper-data
        persistentVolumeClaim:
          claimName: pvc-piper-data
---
apiVersion: v1
kind: Service
metadata:
  name: piper
  namespace: media
  labels:
    app: piper
  annotations:
    lbipam.cilium.io/sharing-key: wyoming-svc
spec:
  ports:
  - name: piper-10200
    port: 10200
    protocol: TCP
    targetPort: 10200
  selector:
    app: piper
  externalTrafficPolicy: Cluster
  type: LoadBalancer
  loadBalancerIP: 10.0.0.247
